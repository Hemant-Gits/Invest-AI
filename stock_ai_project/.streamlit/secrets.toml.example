# Copy to secrets.toml for local dev, or paste into Streamlit Cloud → Settings → Secrets

# Optional — news works without this via Yahoo Finance + Google News RSS
NEWS_API_KEY = "your_newsapi_key_here"
